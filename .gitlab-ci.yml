image: node:latest

stages:
  - build
  - test

cache:
  paths:
    - node_modules/

Install dependencies:
  stage: build
  script:
    - npm install
  artifacts:
    paths:
      - node_modules/

Testing:
  stage: test
  script: npm test
